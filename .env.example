# Local FastAPI address. Frontend access does not require a Supabase account.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# The backend must use its existing local-access configuration.
# No backend settings are changed by the frontend.
